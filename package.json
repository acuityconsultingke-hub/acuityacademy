{
  "scripts": {
    "dev": "<existing dev command> && <new worker dev command>",
    "build": "<existing build command> && <new worker build command>",
    "worker:build": "wrangler build",
    "worker:build:watch": "wrangler watch",
    "worker:dev": "wrangler dev",
    "worker:deploy": "wrangler publish"
  },
  "devDependencies": {
    "wrangler": "<version>",
    "@cloudflare/workers-types": "<version>"
  }
}